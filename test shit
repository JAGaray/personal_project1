const int MAXLOAD = ...
enum {CompSci, Math, Science, etc}
struct Courses 
{
  int id;
  int adinfo[8];
  Subject department;
}

struct Student
{
  string name;
  int courses;
  int grades[MAXLOAD];
  int ???[MAXLOAD];
}

enroll a student of string sname into a course of cid 
you must look up the course info such as adinfo, etc through structs and add them in
